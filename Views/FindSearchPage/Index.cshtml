@using EPiServer.Core
@using EPiServer.Web.Mvc.Html

@model BbmUnderlakare.Models.ViewModels.FindSearchPageViewModel

@*@{
        ViewBag.Title = "Index";
        Layout = "~/Views/Shared/Layouts/_TwoPlusOne.cshtml";
    }*@

<html>
<head>
</head>
<body>
    @using (Html.BeginForm(FormMethod.Post))
    {
        <h1>Sök</h1>
        <input type="text" tabindex="1" name="q" value="@Model.SearchQuery" />
        <input type="submit" tabindex="2" class="btn" value="Search" />
    }


    @if (Model.Results != null)
    {
        <p>Your search for <i> @Model.SearchQuery resulted in @Model.Results.Count() hits</i></p>
        <div class="listResult">
            <ul>
                @foreach (var item in Model.Results)
                {
                    <li>
                        <h4><a href="@item.Url">@item.Title</a></h4>
                        @item.Excerpt
                    </li>
                }
            </ul>
        </div>
    }
</body>
</html>



